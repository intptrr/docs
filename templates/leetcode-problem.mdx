---
title: 0. Problem Title
description: One-line summary of what the problem asks.
difficulty: Easy # Easy | Medium | Hard
id: 0
url: https://leetcode.com/problems/problem-slug/
tags:
  - Tag One
  - Tag Two
---

{/*
  How to use this template:
  1. Copy this file into the right category, e.g.
     content/docs/leetcode/<category>/<problem-slug>.mdx
  2. Fill in the frontmatter (title is "<id>. <Name>") and the body below.
  3. Add the slug to that category's meta.json "pages" array so it shows up
     in the sidebar.
  4. Run `pnpm types:check` to validate frontmatter and regenerate the source.
  Notes: math uses KaTeX ($...$), and multiple languages can be added as extra
  <Tab> entries. Delete these instructions before publishing.
*/}

## Approach: Name Of Approach

Explain the core idea in a sentence or two — the key insight that makes the
solution work.

Algorithm:
1. First step.
2. Second step.
3. Return the result.

<Tabs items={['Python']}>
  <Tab value="Python">
```python
class Solution:
    def solve(self) -> None:
        pass
```
  </Tab>
</Tabs>

- **Time:** $O(n)$ — why.
- **Space:** $O(1)$ — why.
