---
title: Example
description: A kitchen-sink showcase of MDX features — headings, lists, tables, code, quotes, math, and Mermaid.
---

This page demonstrates the formatting primitives available in the docs.

## Headings

### Third-level heading

#### Fourth-level heading

##### Fifth-level heading

## Lists

Unordered:

- First-class support for **bold**, _italic_, and `inline code`.
- Nested items:
  - Sub-item one
  - Sub-item two
    - Even deeper

Ordered:

1. Boot the system
2. Load the kernel
3. Mount the root filesystem
4. Hand off to userspace

Task list:

- [x] Write the example page
- [x] Wire up Mermaid
- [ ] Take over the world

## Tables

| Algorithm     | Time         | Space    | Stable |
| ------------- | ------------ | -------- | :----: |
| Quick Sort    | $O(n\log n)$ | $O(\log n)$ |   ❌   |
| Merge Sort    | $O(n\log n)$ | $O(n)$   |   ✅   |
| Bubble Sort   | $O(n^2)$     | $O(1)$   |   ✅   |
| Counting Sort | $O(n + k)$   | $O(k)$   |   ✅   |

## Code

Inline: use `Array.prototype.reduce` to fold a sequence into a single value.

A TypeScript block with a filename and highlights:

```ts title="binary-search.ts"
export function binarySearch(arr: number[], target: number): number {
  let lo = 0;
  let hi = arr.length - 1;

  while (lo <= hi) {
    const mid = (lo + hi) >>> 1;
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) lo = mid + 1;
    else hi = mid - 1;
  }

  return -1;
}
```

A shell snippet:

```bash
pnpm install
pnpm dev
```

## Quotes

> "Premature optimization is the root of all evil."
> — Donald Knuth

> Nested quotes work too:
>
> > Turtles all the way down.

## Math

Inline math: the Euclidean norm is $\|x\|_2 = \sqrt{\sum_{i=1}^{n} x_i^2}$.

Block math (Cauchy–Schwarz inequality):

$$
\left(\sum_{i=1}^{n} a_i b_i\right)^2 \le \left(\sum_{i=1}^{n} a_i^2\right)\left(\sum_{i=1}^{n} b_i^2\right)
$$

Master theorem recurrence:

$$
T(n) = a\,T\!\left(\frac{n}{b}\right) + f(n)
$$

## Mermaid

Flowchart:

<Mermaid chart={`flowchart LR
  A[Client] -->|HTTP| B(API Gateway)
  B --> C{Auth?}
  C -- yes --> D[Service]
  C -- no  --> E[401]
  D --> F[(Database)]
`} />

Sequence diagram:

<Mermaid chart={`sequenceDiagram
  participant U as User
  participant W as Web
  participant S as Server
  U->>W: Click "Sign in"
  W->>S: POST /login
  S-->>W: 200 OK + cookie
  W-->>U: Redirect to dashboard
`} />

Class diagram:

<Mermaid chart={`classDiagram
  class Animal {
    +String name
    +move() void
  }
  class Dog {
    +bark() void
  }
  Animal <|-- Dog
`} />
